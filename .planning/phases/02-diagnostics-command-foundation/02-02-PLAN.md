---
phase: 02-diagnostics-command-foundation
plan: "02"
type: execute
objective: Add concrete auth, API trust, dependency, and setup readiness checks with actionable guidance.
wave: 2
depends_on:
  - "01"
files_modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
files-modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
must_haves:
  truths:
    - `tavily doctor` reports API key state, credential persistence state, and API URL trust status.
    - `tavily doctor` reports dependency readiness for `node`, `npm`, and `npx`, plus skill/MCP setup readiness checks.
  artifacts:
    - src/commands/doctor/checks.ts
    - src/commands/doctor/report.ts
    - src/commands/doctor.ts
    - src/__tests__/commands/doctor.test.ts
    - src/__tests__/integration/cli.test.ts
  key_links:
    - Auth and API trust checks read from the same config/credentials sources used by runtime commands.
    - Dependency/setup checks surface remediation guidance in both text and JSON report entries.
---

<objective>
Implement the diagnostics checks required for local readiness validation.

Purpose: Deliver meaningful environment/setup diagnosis output beyond command scaffolding.
Output: Concrete check implementations and tests for auth, trust, dependency, and setup readiness.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-diagnostics-command-foundation/02-RESEARCH.md
@src/utils/config.ts
@src/utils/credentials.ts
@src/commands/setup.ts
@src/commands/init.ts
@src/utils/process.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement auth and API trust diagnostics checks</name>
  <files>
src/commands/doctor/checks.ts
src/commands/doctor/report.ts
src/commands/doctor.ts
src/__tests__/commands/doctor.test.ts
  </files>
  <action>
Add check implementations for API key resolution state (env vs stored vs missing), credential file readability/presence, and API URL trust posture (`https` + trusted host or explicit override). Each check must emit a stable check ID, pass/fail/warn status, summary message, and remediation hint that matches the command's existing config/auth behavior. Cover edge cases in unit tests (missing key, malformed credentials file, untrusted host override state).
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/doctor.test.ts</automated>
  </verify>
  <done>
Doctor output includes deterministic auth/trust checks with actionable guidance and passing unit tests for healthy/failure paths.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement dependency and setup readiness checks with guidance</name>
  <files>
src/commands/doctor/checks.ts
src/commands/doctor.ts
src/__tests__/commands/doctor.test.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Add checks for executable availability/version probing of `node`, `npm`, and `npx`, plus setup readiness checks that validate prerequisites for `tavily setup skills` and `tavily setup mcp`. Ensure each failing check includes actionable next steps (for example install command/tooling hints) and that check execution tolerates platform differences without crashing. Extend tests for dependency missing/present scenarios and for command-level rendering of these checks.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/doctor.test.ts src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
Doctor reports dependency and setup readiness across required tools with clear remediation guidance and tested behavior.
  </done>
</task>

</tasks>

<verification>
- Auth/trust diagnostics align with `utils/config` and `utils/credentials` semantics.
- Dependency/setup checks produce deterministic, actionable statuses.
</verification>

<success_criteria>
- Requirements `DIAG-01` and `DIAG-02` are covered by concrete diagnostics checks.
- Check output is stable enough to be consumed by the JSON contract in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-diagnostics-command-foundation/02-02-SUMMARY.md`.
</output>
