---
phase: 02-diagnostics-command-foundation
plan: "03"
type: execute
objective: Implement stable JSON diagnostics reporting and deterministic doctor exit semantics.
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/commands/doctor.ts
  - src/commands/doctor/report.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
  - README.md
files-modified:
  - src/commands/doctor.ts
  - src/commands/doctor/report.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
  - README.md
autonomous: true
requirements:
  - DIAG-03
must_haves:
  truths:
    - `tavily doctor --json` emits a stable machine-readable diagnostics schema suitable for CI/automation parsing.
    - Doctor command exits deterministically based on aggregated check outcomes.
  artifacts:
    - src/commands/doctor.ts
    - src/commands/doctor/report.ts
    - src/__tests__/commands/doctor.test.ts
    - src/__tests__/integration/cli.test.ts
    - README.md
  key_links:
    - JSON output path serializes the same canonical report object used by text output.
    - Integration tests assert CLI exit semantics for all-pass versus failing diagnostics states.
---

<objective>
Finalize diagnostics output contracts for automation and reliable shell integration.

Purpose: Make `doctor` directly usable by CI scripts and release checks.
Output: Stable JSON schema, deterministic exit code mapping, and updated command documentation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-diagnostics-command-foundation/02-RESEARCH.md
@src/utils/output.ts
@src/__tests__/integration/cli.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define and enforce stable doctor JSON schema</name>
  <files>
src/commands/doctor/report.ts
src/commands/doctor.ts
src/__tests__/commands/doctor.test.ts
  </files>
  <action>
Finalize a version-stable JSON report structure for `doctor` (for example: metadata, summary counts, overall status, and per-check records with id/category/status/message/remediation/details). Ensure `--json`, `--pretty`, and `--output` behaviors use existing CLI output conventions and never emit shape variants based on text mode assumptions. Add contract-focused tests that lock required keys and value types.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/doctor.test.ts</automated>
  </verify>
  <done>
`doctor --json` output is deterministic and validated by schema-contract tests.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement deterministic exit code strategy from aggregate status</name>
  <files>
src/commands/doctor.ts
src/__tests__/integration/cli.test.ts
src/__tests__/commands/doctor.test.ts
  </files>
  <action>
Map aggregate diagnostics health to deterministic exit behavior suitable for automation (success when all required checks pass, non-zero when required checks fail). Keep mapping centralized in doctor command flow and consistent across text/JSON outputs. Add tests that assert exit outcomes for all-pass and failing scenarios through CLI integration behavior.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts src/__tests__/commands/doctor.test.ts</automated>
  </verify>
  <done>
Doctor exit behavior is deterministic and covered by automated tests for pass/fail health states.
  </done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Document doctor JSON/exit behavior and run full reliability gates</name>
  <files>
README.md
src/__tests__/commands/doctor.test.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Update README command reference for `tavily doctor` with examples for text output and `--json` automation usage, including exit behavior expectations for CI consumers. Ensure tests remain aligned with documented behavior and run the full project reliability gates before closing the plan.
  </action>
  <verify>
    <automated>pnpm test && pnpm run type-check && pnpm run build</automated>
  </verify>
  <done>
User-facing docs describe stable diagnostics JSON usage and verified exit semantics; full test/type/build gates pass.
  </done>
</task>

</tasks>

<verification>
- JSON contract remains stable across diagnostic states.
- Exit semantics are deterministic and automation-friendly.
</verification>

<success_criteria>
- Requirement `DIAG-03` is fully addressed with stable machine-readable reporting.
- Phase 2 outputs can be consumed by later release-gate/verification workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-diagnostics-command-foundation/02-03-SUMMARY.md`.
</output>
