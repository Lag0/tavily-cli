---
phase: 02-diagnostics-command-foundation
plan: "01"
type: execute
objective: Introduce `tavily doctor` command scaffolding and the diagnostics check framework.
wave: 1
depends_on: []
files_modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/__tests__/integration/cli.test.ts
files-modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/__tests__/integration/cli.test.ts
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - Users can invoke `tavily doctor` from the CLI and trigger a diagnostics execution flow.
    - Diagnostics are represented by typed check contracts that can be extended without redesigning command wiring.
  artifacts:
    - src/commands/doctor.ts
    - src/commands/doctor/checks.ts
    - src/commands/doctor/report.ts
    - src/commands/registrars/register-tooling-commands.ts
    - src/__tests__/integration/cli.test.ts
  key_links:
    - Tooling registrar wires the `doctor` command to `handleDoctorCommand`.
    - `handleDoctorCommand` delegates check execution/aggregation through the diagnostics check framework.
---

<objective>
Create the diagnostics command foundation and typed check framework for Phase 2.

Purpose: Establish a stable implementation base for auth/setup checks and JSON reporting.
Output: New `doctor` command module, check contracts, and CLI routing test coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-diagnostics-command-foundation/02-RESEARCH.md
@src/commands/registrars/register-tooling-commands.ts
@src/commands/runtime/command-context.ts
@src/utils/output.ts
@src/__tests__/integration/cli.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement typed diagnostics check contracts and report builder</name>
  <files>
src/commands/doctor/checks.ts
src/commands/doctor/report.ts
src/commands/doctor.ts
  </files>
  <action>
Create a diagnostics check framework with explicit types for check IDs, categories, statuses, result payload, and report summary. Implement a command entrypoint (`handleDoctorCommand`) that executes a registry of checks and builds one canonical report object used by both JSON and text output modes. Keep this task focused on framework/scaffolding; detailed environment checks are added in later plans.
  </action>
  <verify>
    <automated>pnpm run type-check</automated>
  </verify>
  <done>
`doctor` command internals compile with typed check/report contracts and can execute at least placeholder checks through a shared aggregator.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Register `tavily doctor` command and lock routing behavior</name>
  <files>
src/commands/registrars/register-tooling-commands.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Add a `doctor` subcommand to the tooling registrar with output flags consistent with existing commands (`--json`, `--pretty`, `--output`). Wire command options to `handleDoctorCommand` without changing existing `init`, `setup`, or `env` behavior. Extend integration tests to assert the new command is routed correctly and remains isolated from existing command handlers.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
`tavily doctor` is routable from CLI and integration tests confirm no regression in existing registrar wiring.
  </done>
</task>

</tasks>

<verification>
- `tavily doctor` is registered and dispatches to command handler.
- Diagnostics framework exists with typed contracts and report aggregation.
</verification>

<success_criteria>
- Phase plan requirement `DIAG-01` has command-level foundation coverage.
- Follow-on plans can add concrete checks without changing command registration shape.
</success_criteria>

<output>
After completion, create `.planning/phases/02-diagnostics-command-foundation/02-01-SUMMARY.md`.
</output>
