---
phase: 04-release-hardening-and-auto-remediation
plan: "01"
type: execute
objective: Implement `tavily doctor --fix` with explicit safe remediation guards and deterministic fix reporting.
wave: 1
depends_on: []
files_modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/commands/doctor/fixes.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
files-modified:
  - src/commands/doctor.ts
  - src/commands/doctor/checks.ts
  - src/commands/doctor/report.ts
  - src/commands/doctor/fixes.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/integration/cli.test.ts
autonomous: true
requirements:
  - DIAG-04
must_haves:
  truths:
    - `tavily doctor --fix` applies only allowlisted local remediations and reports every attempted action.
    - Remediation logic is deterministic and safe (no hidden global installs, no silent destructive mutations).
  artifacts:
    - src/commands/doctor.ts
    - src/commands/doctor/checks.ts
    - src/commands/doctor/report.ts
    - src/commands/doctor/fixes.ts
    - src/commands/registrars/register-tooling-commands.ts
    - src/__tests__/commands/doctor.test.ts
    - src/__tests__/integration/cli.test.ts
  key_links:
    - Tooling registrar forwards `--fix` options to the doctor command handler.
    - Doctor command executes checks, dispatches fix handlers by `DoctorCheckId`, then emits structured results in text/JSON output.
    - Doctor command tests and CLI integration tests lock fix safety behavior and exit semantics.
---

<objective>
Implement safe automatic remediations for doctor diagnostics.

Purpose: satisfy DIAG-04 while preserving CLI safety/reliability constraints.
Output: `doctor --fix` command path with allowlisted fix actions, guardrails, and regression tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-release-hardening-and-auto-remediation/04-RESEARCH.md
@src/commands/doctor.ts
@src/commands/doctor/checks.ts
@src/commands/doctor/report.ts
@src/commands/registrars/register-tooling-commands.ts
@src/__tests__/commands/doctor.test.ts
@src/__tests__/integration/cli.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define fix-option contracts and fix dispatch scaffolding for doctor</name>
  <files>
src/commands/doctor.ts
src/commands/doctor/fixes.ts
src/commands/registrars/register-tooling-commands.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Add `doctor` CLI options for fix execution (`--fix`, scoped fix selection, and a non-mutating preview mode) and wire them through tooling registrar into the doctor handler. Create a dedicated `fixes.ts` module that defines typed fix dispatch contracts keyed by existing doctor check IDs so remediation logic is isolated from check evaluation and can be audited/tested independently.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts src/__tests__/commands/doctor.test.ts</automated>
  </verify>
  <done>
Doctor fix options are routable from CLI and fix dispatch contracts compile as typed check-ID keyed interfaces.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement allowlisted safe fix actions for selected failing checks</name>
  <files>
src/commands/doctor/fixes.ts
src/commands/doctor/checks.ts
src/commands/doctor.ts
src/__tests__/commands/doctor.test.ts
  </files>
  <action>
Implement safe remediation handlers for selected local setup failures (for example credential-file permission repair, malformed credential-file recovery with backup, and trusted API URL reset when the invalid value originates from stored credentials). Ensure non-fixable checks return explicit `skipped` results with actionable messaging instead of attempting unsafe system-level changes.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/doctor.test.ts</automated>
  </verify>
  <done>
`doctor --fix` applies only allowlisted local remediations, skips unsupported fixes deterministically, and preserves safety guardrails.
  </done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Extend doctor reporting to include fix outcomes and deterministic exit behavior</name>
  <files>
src/commands/doctor.ts
src/commands/doctor/report.ts
src/__tests__/commands/doctor.test.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Extend doctor text/JSON reporting contracts to include fix attempt outcomes (`applied`, `skipped`, `failed`, and reason) while preserving existing diagnostics schema compatibility. Keep exit-code mapping deterministic: diagnostics failures unresolved after fix run should remain non-zero; successful remediations should surface clearly in output and tests.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/doctor.test.ts src/__tests__/integration/cli.test.ts && pnpm run type-check</automated>
  </verify>
  <done>
Fix outcomes are surfaced in doctor output contracts and automated tests lock deterministic post-fix exit semantics.
  </done>
</task>

</tasks>

<verification>
- `doctor --fix` path is safe-by-default and constrained to explicit local remediations.
- Fix execution/reporting behavior is deterministic across text and JSON modes.
</verification>

<success_criteria>
- Requirement `DIAG-04` is satisfied by a validated fix-action implementation with safety guards.
- Existing doctor diagnostics behavior remains stable while extending fix capability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-hardening-and-auto-remediation/04-01-SUMMARY.md`.
</output>
