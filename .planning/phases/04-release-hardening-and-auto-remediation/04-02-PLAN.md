---
phase: 04-release-hardening-and-auto-remediation
plan: "02"
type: execute
objective: Add smoke-aware release gates so publish is blocked unless reliability validation and build quality checks are green.
wave: 2
depends_on:
  - "01"
files_modified:
  - package.json
  - scripts/release/smoke.mjs
  - .github/workflows/test.yml
  - .github/workflows/publish.yml
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/integration/cli-routing-regression.test.ts
files-modified:
  - package.json
  - scripts/release/smoke.mjs
  - .github/workflows/test.yml
  - .github/workflows/publish.yml
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/integration/cli-routing-regression.test.ts
autonomous: true
requirements:
  - RELI-04
  - REL-01
must_haves:
  truths:
    - Release validation includes a deterministic smoke gate over critical CLI reliability paths.
    - npm publish workflow cannot publish when type-check, tests, build, or smoke checks fail.
  artifacts:
    - package.json
    - scripts/release/smoke.mjs
    - .github/workflows/test.yml
    - .github/workflows/publish.yml
    - src/__tests__/integration/cli.test.ts
    - src/__tests__/integration/cli-routing-regression.test.ts
  key_links:
    - `pnpm run smoke` is defined in package scripts and executed in both CI and publish workflows.
    - Publish job orders quality gates before `npm publish` and fails fast when any gate fails.
    - Smoke command validates the same critical command-path assumptions covered by integration tests.
---

<objective>
Harden release workflow gates with deterministic smoke validation.

Purpose: satisfy RELI-04 and REL-01 by preventing publish when reliability/build/test gates regress.
Output: reusable smoke command + workflow-enforced release gate chain.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-release-hardening-and-auto-remediation/04-RESEARCH.md
@package.json
@.github/workflows/test.yml
@.github/workflows/publish.yml
@src/__tests__/integration/cli.test.ts
@src/__tests__/integration/cli-routing-regression.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create release smoke command and package script entrypoint</name>
  <files>
package.json
scripts/release/smoke.mjs
  </files>
  <action>
Add a deterministic smoke command that validates critical release-facing CLI behavior on built artifacts (including doctor diagnostics path and routing reliability checks). Expose this as `pnpm run smoke` so local pre-release checks and CI use one canonical entrypoint.
  </action>
  <verify>
    <automated>pnpm run build && pnpm run smoke</automated>
  </verify>
  <done>
Smoke validation runs through a single scripted command and passes on a healthy build.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Enforce smoke and existing quality gates in CI and publish workflows</name>
  <files>
.github/workflows/test.yml
.github/workflows/publish.yml
package.json
  </files>
  <action>
Update workflow jobs so type-check, tests, build, and smoke all run before publish actions. Keep gate ordering explicit and fail-fast; `npm publish` must stay conditionally unreachable when any prior gate fails.
  </action>
  <verify>
    <automated>pnpm run type-check && pnpm run test && pnpm run build && pnpm run smoke</automated>
  </verify>
  <done>
Release workflow includes smoke gate and publish cannot proceed when any mandatory quality gate fails.
  </done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Expand reliability regression assertions aligned with smoke contract</name>
  <files>
src/__tests__/integration/cli.test.ts
src/__tests__/integration/cli-routing-regression.test.ts
scripts/release/smoke.mjs
  </files>
  <action>
Add or tighten integration assertions around critical command paths exercised by smoke validation (doctor path, command routing invariants, and deterministic error-boundary behavior). Keep smoke scope focused and stable so release checks remain fast and non-flaky.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts src/__tests__/integration/cli-routing-regression.test.ts && pnpm run smoke</automated>
  </verify>
  <done>
Smoke coverage reflects critical reliability paths with deterministic automated assertions.
  </done>
</task>

</tasks>

<verification>
- Smoke command exists, is CI-usable, and validates release-critical CLI behavior.
- Publish workflow blocks release when any mandatory gate fails.
</verification>

<success_criteria>
- Requirement `RELI-04` is addressed by release-smoke reliability coverage.
- Requirement `REL-01` is addressed by enforced publish gate chain.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-hardening-and-auto-remediation/04-02-SUMMARY.md`.
</output>
