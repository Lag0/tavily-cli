---
phase: 01-architecture-baseline-refactor
plan: "02"
type: execute
objective: Introduce shared runtime context/helpers for web command execution flow.
wave: 2
depends_on:
  - "01"
files_modified:
  - src/commands/runtime/command-context.ts
  - src/commands/runtime/with-command-handler.ts
  - src/commands/search.ts
  - src/commands/extract.ts
  - src/commands/crawl.ts
  - src/commands/map.ts
  - src/commands/research.ts
  - src/__tests__/commands/search.test.ts
  - src/__tests__/commands/extract.test.ts
files-modified:
  - src/commands/runtime/command-context.ts
  - src/commands/runtime/with-command-handler.ts
  - src/commands/search.ts
  - src/commands/extract.ts
  - src/commands/crawl.ts
  - src/commands/map.ts
  - src/commands/research.ts
  - src/__tests__/commands/search.test.ts
  - src/__tests__/commands/extract.test.ts
autonomous: true
requirements:
  - ARCH-03
must_haves:
  truths:
    - Shared runtime helpers remove duplicated command execution scaffolding in web command handlers.
    - Command handlers keep command-specific formatting while reusing common client/output flow.
  artifacts:
    - src/commands/runtime/command-context.ts
    - src/commands/runtime/with-command-handler.ts
    - src/commands/search.ts
    - src/commands/extract.ts
    - src/commands/crawl.ts
    - src/commands/map.ts
    - src/commands/research.ts
  key_links:
    - Web command handlers use runtime helpers for shared client/output/error scaffolding.
    - Existing command option mapping tests still pass after helper adoption.
---

<objective>
Introduce a reusable runtime context/helper layer for command execution.

Purpose: Eliminate duplicated client/output orchestration patterns that currently repeat across web-oriented commands.
Output: Shared runtime helpers integrated into web command handlers with preserved behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-architecture-baseline-refactor/01-RESEARCH.md
@src/commands/search.ts
@src/commands/extract.ts
@src/commands/crawl.ts
@src/commands/map.ts
@src/commands/research.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create command runtime context and wrapper helpers</name>
  <files>
src/commands/runtime/command-context.ts
src/commands/runtime/with-command-handler.ts
  </files>
  <action>
Implement a shared command runtime layer that encapsulates repeated concerns currently spread across web command handlers: resolving Tavily client from options, detecting JSON/file output mode, and writing output consistently. Expose a typed wrapper function that executes command bodies and returns/throws normalized errors without calling `process.exit`. Keep helper contracts narrow and compatible with existing command option types.
  </action>
  <verify>
    <automated>pnpm run type-check</automated>
  </verify>
  <done>
Runtime helper files export typed primitives that can be consumed by command handlers to avoid duplicated setup/output scaffolding.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Refactor web command handlers to use shared runtime helper</name>
  <files>
src/commands/search.ts
src/commands/extract.ts
src/commands/crawl.ts
src/commands/map.ts
src/commands/research.ts
src/__tests__/commands/search.test.ts
src/__tests__/commands/extract.test.ts
  </files>
  <action>
Update web command handlers (`search`, `extract`, `crawl`, `map`, `research`) to call the shared runtime helper for common execution concerns while preserving command-specific request mapping and human-readable formatters. Keep exported `execute*` functions stable for existing tests; adjust unit tests only where helper-driven behavior changes are intentional.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/search.test.ts src/__tests__/commands/extract.test.ts src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
Web command handlers no longer duplicate client/output scaffolding logic and still pass mapping/integration tests.
  </done>
</task>

</tasks>

<verification>
- Shared helper is used by all web command handlers listed in this plan.
- Command option-to-client mapping remains validated by targeted tests.
</verification>

<success_criteria>
- Phase plan requirement `ARCH-03` is addressed by concrete shared runtime utility adoption.
- Duplicated command runtime scaffolding is reduced in touched handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-baseline-refactor/01-02-SUMMARY.md`.
</output>
