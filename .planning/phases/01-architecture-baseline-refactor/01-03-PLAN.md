---
phase: 01-architecture-baseline-refactor
plan: "03"
type: execute
objective: Centralize error-to-exit translation at the CLI boundary.
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/index.ts
  - src/commands/runtime/command-error.ts
  - src/commands/search.ts
  - src/commands/extract.ts
  - src/commands/crawl.ts
  - src/commands/map.ts
  - src/commands/research.ts
  - src/commands/login.ts
  - src/commands/init.ts
  - src/commands/setup.ts
  - src/commands/env.ts
  - src/utils/process.ts
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/commands/login.test.ts
  - src/__tests__/commands/init.test.ts
files-modified:
  - src/index.ts
  - src/commands/runtime/command-error.ts
  - src/commands/search.ts
  - src/commands/extract.ts
  - src/commands/crawl.ts
  - src/commands/map.ts
  - src/commands/research.ts
  - src/commands/login.ts
  - src/commands/init.ts
  - src/commands/setup.ts
  - src/commands/env.ts
  - src/utils/process.ts
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/commands/login.test.ts
  - src/__tests__/commands/init.test.ts
autonomous: true
requirements:
  - RELI-03
  - ARCH-03
must_haves:
  truths:
    - Exit code translation happens at the CLI boundary rather than in deep command/helper modules.
    - Command failures still produce actionable error output and stable non-zero exit behavior.
  artifacts:
    - src/commands/runtime/command-error.ts
    - src/index.ts
    - src/commands/login.ts
    - src/commands/init.ts
    - src/commands/setup.ts
    - src/commands/env.ts
    - src/utils/process.ts
  key_links:
    - Command modules throw typed runtime errors consumed by `runCli` boundary mapping.
    - CLI tests assert non-zero semantics via boundary handling instead of deep `process.exit` calls.
---

<objective>
Centralize error-to-exit translation at the CLI boundary.

Purpose: Fulfill RELI-03 by removing deep-module forced termination and keeping exit semantics deterministic.
Output: Typed runtime error model + boundary mapper in `runCli` + updated tests for failure paths.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-architecture-baseline-refactor/01-RESEARCH.md
@src/index.ts
@src/utils/process.ts
@src/commands/login.ts
@src/commands/init.ts
@src/commands/setup.ts
@src/commands/env.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add runtime error contract and boundary exit mapper</name>
  <files>
src/commands/runtime/command-error.ts
src/index.ts
  </files>
  <action>
Create a typed command runtime error contract (for example: `code`, `message`, `suggestion`, `exitCode`, optional `cause`) and add a single mapper in CLI boundary code (`runCli`/entrypoint catch path) that translates thrown errors into `process.exitCode` and user-facing stderr output. Keep one top-level process termination path and avoid direct termination in handlers.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
CLI boundary owns exit-code assignment for command/runtime failures, and runtime error type is available to command/helper modules.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Remove deep-module `process.exit` usage by throwing runtime errors</name>
  <files>
src/commands/search.ts
src/commands/extract.ts
src/commands/crawl.ts
src/commands/map.ts
src/commands/research.ts
src/commands/login.ts
src/commands/init.ts
src/commands/setup.ts
src/commands/env.ts
src/utils/process.ts
  </files>
  <action>
Replace deep `process.exit(...)` calls in command modules and process utility with runtime-error throws consumed by the new boundary mapper. Preserve existing error messages and failure semantics, but ensure command/helper modules no longer forcefully terminate the process themselves.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/login.test.ts src/__tests__/commands/init.test.ts src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
No deep command/helper module uses `process.exit`; failures are expressed through thrown runtime errors and handled centrally.
  </done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Validate full reliability boundary behavior</name>
  <files>
src/__tests__/integration/cli.test.ts
src/__tests__/commands/login.test.ts
src/__tests__/commands/init.test.ts
  </files>
  <action>
Update tests to assert centralized non-zero behavior still works when command failures occur (invalid options, missing auth setup, installer failures). Confirm tests no longer depend on deep-module direct exits and instead validate boundary-mapped failure outcomes.
  </action>
  <verify>
    <automated>pnpm test && pnpm run type-check && pnpm run build</automated>
  </verify>
  <done>
Test suite confirms centralized CLI boundary exit handling with stable failure behavior.
  </done>
</task>

</tasks>

<verification>
- `process.exit` is only used at CLI boundary.
- Failure semantics remain stable and tested.
</verification>

<success_criteria>
- `RELI-03` is satisfied by centralized exit behavior.
- Shared runtime utilities/error model continue to support `ARCH-03`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-baseline-refactor/01-03-SUMMARY.md`.
</output>
