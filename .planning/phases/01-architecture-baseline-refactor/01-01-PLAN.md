---
phase: 01-architecture-baseline-refactor
plan: "01"
type: execute
objective: Extract command registration from index into modular domain registrars.
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/commands/registrars/register-web-commands.ts
  - src/commands/registrars/register-auth-commands.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/commands/registrars/register-all-commands.ts
  - src/__tests__/integration/cli.test.ts
files-modified:
  - src/index.ts
  - src/commands/registrars/register-web-commands.ts
  - src/commands/registrars/register-auth-commands.ts
  - src/commands/registrars/register-tooling-commands.ts
  - src/commands/registrars/register-all-commands.ts
  - src/__tests__/integration/cli.test.ts
autonomous: true
requirements:
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - Command registration is split by domain and composed from one aggregator.
    - Existing user commands remain routable through the same CLI entrypoint.
  artifacts:
    - src/commands/registrars/register-web-commands.ts
    - src/commands/registrars/register-auth-commands.ts
    - src/commands/registrars/register-tooling-commands.ts
    - src/commands/registrars/register-all-commands.ts
    - src/index.ts
  key_links:
    - src/index.ts imports register-all-commands and delegates command wiring to registrars.
    - Registrar modules bind existing handlers with unchanged command names and options.
---

<objective>
Extract command registration from `src/index.ts` into domain registrars.

Purpose: Remove the single oversized routing bottleneck while preserving the current command surface.
Output: Modular registrar files + updated CLI integration coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-architecture-baseline-refactor/01-RESEARCH.md
@src/index.ts
@src/__tests__/integration/cli.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create domain registrars and move command definitions out of index</name>
  <files>
src/commands/registrars/register-web-commands.ts
src/commands/registrars/register-auth-commands.ts
src/commands/registrars/register-tooling-commands.ts
src/commands/registrars/register-all-commands.ts
src/index.ts
  </files>
  <action>
Create registrar modules that each receive a Commander `program` and register one command domain: web commands (`search`, `extract`, `crawl`, `map`, `research`, `research-status`), auth/status commands (`login`, `logout`, `status`), and tooling commands (`init`, `setup`, `env`). Keep parser helpers and preAction hook in `index.ts`; only move command registration blocks. Add `registerAllCommands(program)` aggregator and call it from `createProgram()`. Preserve command names, options, and action payload mapping exactly.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
`createProgram()` no longer contains inline `.command(...)` blocks for individual commands, and registrars contain the command wiring with equivalent behavior.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Lock routing compatibility with targeted integration updates</name>
  <files>src/__tests__/integration/cli.test.ts</files>
  <action>
Expand/adjust integration tests to assert that moved registrations still route correctly and option parsing/validation behavior remains unchanged. Keep existing tests and add focused checks that at least one command from each registrar domain executes the expected mocked handler.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
Integration coverage demonstrates no command-routing regression after modular registrar extraction.
  </done>
</task>

</tasks>

<verification>
- `createProgram()` delegates command registration via `registerAllCommands`.
- Routing tests pass after extraction.
</verification>

<success_criteria>
- Phase plan requirement `ARCH-01` is satisfied for command registration modularity.
- No command name or option signature regression is introduced by the split.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-baseline-refactor/01-01-SUMMARY.md`.
</output>
