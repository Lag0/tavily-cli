---
phase: 03-reliability-and-type-safe-contracts
plan: "01"
type: execute
objective: Define a shared CLI runtime error model and apply deterministic API/network failure classification.
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/commands/runtime/command-error.ts
  - src/commands/runtime/with-command-handler.ts
  - src/commands/runtime/classify-command-error.ts
  - src/commands/runtime/render-cli-error.ts
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/commands/runtime/with-command-handler.test.ts
files-modified:
  - src/index.ts
  - src/commands/runtime/command-error.ts
  - src/commands/runtime/with-command-handler.ts
  - src/commands/runtime/classify-command-error.ts
  - src/commands/runtime/render-cli-error.ts
  - src/__tests__/integration/cli.test.ts
  - src/__tests__/commands/runtime/with-command-handler.test.ts
autonomous: true
requirements:
  - RELI-01
  - RELI-02
must_haves:
  truths:
    - Command failures render one standardized format with stable code, message, and suggested remediation.
    - Tavily API/network failures are classified consistently and map to deterministic non-zero exit behavior.
  artifacts:
    - src/index.ts
    - src/commands/runtime/command-error.ts
    - src/commands/runtime/with-command-handler.ts
    - src/commands/runtime/classify-command-error.ts
    - src/commands/runtime/render-cli-error.ts
    - src/__tests__/integration/cli.test.ts
    - src/__tests__/commands/runtime/with-command-handler.test.ts
  key_links:
    - `withCommandHandler` routes unknown thrown values through runtime classification before rethrowing `CommandRuntimeError`.
    - `handleCliError` uses one shared renderer for all runtime error outputs and exit code handling.
---

<objective>
Create the runtime reliability contract for phase 3.

Purpose: guarantee consistent error semantics and deterministic Tavily transport failure behavior before typed adapter migration.
Output: centralized runtime error classification + formatting with regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reliability-and-type-safe-contracts/03-RESEARCH.md
@src/index.ts
@src/commands/runtime/command-error.ts
@src/commands/runtime/with-command-handler.ts
@src/__tests__/integration/cli.test.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Introduce a standardized CLI runtime error renderer and contract</name>
  <files>
src/commands/runtime/command-error.ts
src/commands/runtime/render-cli-error.ts
src/index.ts
  </files>
  <action>
Extend the runtime error contract to support a consistent display surface for all command failures (stable code + message + suggestion/remediation + optional cause detail for debug context). Add one renderer helper used by `handleCliError` so every runtime error path shares identical formatting and no command prints bespoke error shapes.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
CLI runtime errors are rendered through one centralized formatter and integration tests confirm stable exit/error handling behavior.
  </done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Classify Tavily API and network failures into deterministic runtime errors</name>
  <files>
src/commands/runtime/classify-command-error.ts
src/commands/runtime/with-command-handler.ts
src/commands/runtime/command-error.ts
src/__tests__/commands/runtime/with-command-handler.test.ts
src/__tests__/integration/cli.test.ts
  </files>
  <action>
Implement runtime classification that maps transport/network/API-shaped failures to stable `CommandRuntimeError` codes and remediation guidance (for example retry/network guidance on transient failures and clear action hints for auth/config/API issues). Keep classification in runtime boundary helpers so all web commands inherit consistent behavior without duplicated catch logic.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/commands/runtime/with-command-handler.test.ts src/__tests__/integration/cli.test.ts</automated>
  </verify>
  <done>
Tavily-facing runtime failures are consistently classified and surfaced with deterministic exit semantics across command paths.
  </done>
</task>

</tasks>

<verification>
- Error rendering uses one shared runtime formatter.
- API/network failures are normalized to stable command error categories and suggestions.
</verification>

<success_criteria>
- Requirements `RELI-01` and `RELI-02` are covered by runtime error normalization and classification.
- Follow-on typed adapter work can rely on stable runtime failure semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reliability-and-type-safe-contracts/03-01-SUMMARY.md`.
</output>
